<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sk.tuke.ms.sedentti">

    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service
            android:name=".ActivityRecognitionService"
            android:enabled="true"
            android:exported="true"></service>

        <activity android:name=".StatusActivity" />
        <activity android:name=".SessionHistoryActivity" />
        <activity android:name=".MotivateMeActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--        <receiver-->
        <!--            android:name=".activity.recognition.ActivityRecognitionBroadcastReceiver"-->
        <!--            android:exported="false"-->
        <!--            android:permission="com.google.android.gms.permission.ACTIVITY_RECOGNITION">-->
        <!--            <intent-filter>-->
        <!--                <action android:name="sk.tuke.ms.sedentti.activity.recognition.ACTION_PROCESS_ACTIVITY_TRANSITION" />-->
        <!--            </intent-filter>-->
        <!--        </receiver>-->
    </application>

</manifest>